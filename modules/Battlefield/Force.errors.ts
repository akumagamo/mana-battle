export const FORCE_SQUAD_MAX_SIZE = "More than 5 units were provided"
export const FORCE_SQUAD_MIN_SIZE = "No units were provided"
export const UNIT_NOT_IN_BENCH = (id: string) =>
    `Unit ${id} should be in the bench.`
export const SQUAD_NOT_BENCHED = (id: string) =>
    `Squad ${id} should be in the bench`
export const SQUAD_NOT_DISPATCHED = (id: string) =>
    `Squad ${id} should be dispatched`
