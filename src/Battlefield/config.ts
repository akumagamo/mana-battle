import { GAME_SPEED } from '../env';

export const cellSize = 100;
export const boardPadding = 50;

export const CHARA_MAP_SCALE = 1;

export const MOVE_SPEED = 0.4 * GAME_SPEED;
export const CHARA_VERTICAL_OFFSET = 0;
